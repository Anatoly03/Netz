
// TODO add debugging tags and improve testing
// TODO current tests only check for compilation

struct Foo {
    small: u8;
}

enum Bar {
    Small(u8) = 1;
    Medium(u16) = 2;
    Large(u32) = 3;
}

struct FooBar {
    Foo[];
    Bar;
}
